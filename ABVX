-- ==================================================
-- ABVX LOADER & UNIVERSAL RAYCAST SILENT AIM & CAMLOCK [PART 1/8]
-- ==================================================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local StarterGui = game:GetService("StarterGui")
local GuiService = game:GetService("GuiService")
local LocalPlayer = Players.LocalPlayer

if not LocalPlayer then
    Players:GetPropertyChangedSignal("LocalPlayer"):Wait()
    LocalPlayer = Players.LocalPlayer
end

local camera = workspace.CurrentCamera

if CoreGui:FindFirstChild("AliiwydddUltimateSilentAim") then
    CoreGui.AliiwydddUltimateSilentAim:Destroy()
end
if CoreGui:FindFirstChild("AliiwydddEmoteHub") then
    CoreGui.AliiwydddEmoteHub:Destroy()
end
if CoreGui:FindFirstChild("KeyUI") then
    CoreGui.KeyUI:Destroy()
end

local keyScreenGui = Instance.new("ScreenGui")
keyScreenGui.Name = "KeyUI"
keyScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
keyScreenGui.ResetOnSpawn = false
keyScreenGui.Parent = CoreGui

local keyFrame = Instance.new("Frame", keyScreenGui)
keyFrame.Size = UDim2.new(0, 500, 0, 300)
keyFrame.Position = UDim2.new(0.5, -250, 0.5, -150)
keyFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
keyFrame.BorderSizePixel = 0
keyFrame.Active = true
keyFrame.Draggable = true

local topBar = Instance.new("Frame", keyFrame)
topBar.Size = UDim2.new(1, 0, 0, 30)
topBar.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
topBar.BorderSizePixel = 0

local iconLabel = Instance.new("ImageLabel", topBar)
iconLabel.Size = UDim2.new(0, 25, 0, 25)
iconLabel.Position = UDim2.new(0, 5, 0, 2)
iconLabel.Image = "rbxassetid://110314637434659"
iconLabel.BackgroundTransparency = 1

local titleText = Instance.new("TextLabel", topBar)
titleText.Text = "Abvx Loader"
titleText.Font = Enum.Font.Code
titleText.TextSize = 14
titleText.TextColor3 = Color3.fromRGB(255, 220, 220)
titleText.BackgroundTransparency = 1
titleText.Position = UDim2.new(0, 35, 0, 6)
titleText.Size = UDim2.new(0, 200, 0, 20)
titleText.TextXAlignment = Enum.TextXAlignment.Left

local closeBtn = Instance.new("TextButton", topBar)
closeBtn.Size = UDim2.new(0, 30, 0, 30)
closeBtn.Position = UDim2.new(1, -30, 0, 0)
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.fromRGB(255, 220, 220)
closeBtn.BackgroundTransparency = 1
closeBtn.TextScaled = true
closeBtn.Font = Enum.Font.Code

closeBtn.MouseButton1Click:Connect(function()
    keyScreenGui:Destroy()
end)

local asciiArt = Instance.new("TextLabel", keyFrame)
asciiArt.Size = UDim2.new(1, 0, 0, 80)
asciiArt.Position = UDim2.new(0.5, -210, 0, 40)
asciiArt.BackgroundTransparency = 1
asciiArt.Font = Enum.Font.Code
asciiArt.TextColor3 = Color3.fromRGB(255, 220, 220)
asciiArt.TextXAlignment = Enum.TextXAlignment.Left
asciiArt.TextYAlignment = Enum.TextYAlignment.Top
asciiArt.TextSize = 12
asciiArt.Text = "    _     _                 _                 \n   / \\   | |__  _ __ ___   | | ___   __ _ ___ \n  / _ \\  | '_ ` _ ` _ ` _ \\  | |/ _ \\ / _` / __|\n / ___ \\ | |_) | | | | | | | | | (_) | (_| \\__ \\\n/_/   \\_\\|_.__/|_| |_| |_| |_|\\___/ \\__,_|___/\n"
-- ================= [END OF PART 1/8] =================

-- ==================================================
-- ABVX LOADER & UNIVERSAL RAYCAST SILENT AIM & CAMLOCK [PART 2/8]
-- ==================================================
local keyPromptLabel = Instance.new("TextLabel", keyFrame)
keyPromptLabel.Position = UDim2.new(0, 20, 0, 140)
keyPromptLabel.Size = UDim2.new(0, 100, 0, 20)
keyPromptLabel.BackgroundTransparency = 1
keyPromptLabel.Text = '<font color="rgb(255,140,140)">[ - ]</font> Key:'
keyPromptLabel.Font = Enum.Font.Code
keyPromptLabel.TextSize = 14
keyPromptLabel.TextXAlignment = Enum.TextXAlignment.Left
keyPromptLabel.TextColor3 = Color3.fromRGB(255, 220, 220)
keyPromptLabel.RichText = true

local keyTextBox = Instance.new("TextBox", keyFrame)
keyTextBox.Position = UDim2.new(0, 110, 0, 140)
keyTextBox.Size = UDim2.new(0, 200, 0, 20)
keyTextBox.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
keyTextBox.BorderSizePixel = 0
keyTextBox.TextColor3 = Color3.fromRGB(255, 220, 220)
keyTextBox.Font = Enum.Font.Code
keyTextBox.TextSize = 14
keyTextBox.ClearTextOnFocus = false
keyTextBox.Text = ""

local statusLabel = Instance.new("TextLabel", keyFrame)
statusLabel.Position = UDim2.new(0, 20, 0, 180)
statusLabel.Size = UDim2.new(0, 400, 0, 20)
statusLabel.BackgroundTransparency = 1
statusLabel.Font = Enum.Font.Code
statusLabel.TextSize = 14
statusLabel.TextXAlignment = Enum.TextXAlignment.Left
statusLabel.RichText = true
statusLabel.TextColor3 = Color3.fromRGB(255, 220, 220)
statusLabel.Text = ""

local timerLabel = Instance.new("TextLabel", keyFrame)
timerLabel.Position = UDim2.new(0.5, -100, 1, -30)
timerLabel.Size = UDim2.new(0, 200, 0, 25)
timerLabel.BackgroundTransparency = 1
timerLabel.Font = Enum.Font.Code
timerLabel.TextSize = 14
timerLabel.TextXAlignment = Enum.TextXAlignment.Center
timerLabel.TextColor3 = Color3.fromRGB(255, 220, 220)
timerLabel.Text = ""

keyTextBox.FocusLost:Connect(function(enterPressed)
    if enterPressed then
        if keyTextBox.Text ~= "Ali" then
            statusLabel.Text = '<font color="rgb(255,140,140)">[ - ]</font> Invalid Key!'
            timerLabel.Text = ""
        else
            statusLabel.Text = '<font color="rgb(140,255,140)">[ + ]</font> Authenticated!'
            task.wait(1)
            timerLabel.Text = "Closing panel in 3..."
            task.wait(1)
            timerLabel.Text = "Closing panel in 2..."
            task.wait(1)
            timerLabel.Text = "Closing panel in 1..."
            task.wait(0.5)
            keyScreenGui:Destroy()

            StarterGui:SetCore("SendNotification", {
                Title = "Abvx Loader",
                Text = "Silent Aim & Camlock Enabled (Rainbow Red/Black)",
                Duration = 5,
            })

            local v79 = {
                Aiming = {
                    Enabled = true,
                    CamlockEnabled = false,
                    PredictionX = 0.03,
                    PredictionY = 0.03,
                    Smoothness = 10,
                    FOV = 160,
                    FOVCircle = true,
                    HitPart = "HumanoidRootPart",
                    TargetMode = "Closest",
                    Prediction = true,
                    ShowInfo = true,
                    ShowTracers = true
                }
            }
            getgenv().CrackThis = v79
-- ================= [END OF PART 2/8] =================

-- ==================================================
-- ABVX LOADER & UNIVERSAL RAYCAST SILENT AIM & CAMLOCK [PART 3/8]
-- ==================================================
            local screenGui = Instance.new("ScreenGui")
            screenGui.Name = "AliiwydddUltimateSilentAim"
            screenGui.ResetOnSpawn = false
            screenGui.IgnoreGuiInset = true
            screenGui.Parent = CoreGui

            local Settings = {
                CircleColor = Color3.fromRGB(255, 0, 0),
                MenuAlign = "Left"
            }

            local isGuiOpen = true

            local function makeDraggable(btn)
                local dragging = false
                local dragStart, startPos

                btn.InputBegan:Connect(function(input)
                    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                        dragging = true
                        dragStart = input.Position
                        startPos = btn.Position
                        
                        input.Changed:Connect(function()
                            if input.UserInputState == Enum.UserInputState.End then
                                dragging = false
                            end
                        end)
                    end
                end)

                btn.InputChanged:Connect(function(input)
                    if (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) and dragging then
                        local delta = input.Position - dragStart
                        btn.Position = UDim2.new(
                            startPos.X.Scale, 
                            startPos.X.Offset + delta.X, 
                            startPos.Y.Scale, 
                            startPos.Y.Offset + delta.Y
                        )
                    end
                end)
            end

            local fovCircleFrame = Instance.new("Frame")
            fovCircleFrame.Name = "FOVCircle"
            fovCircleFrame.AnchorPoint = Vector2.new(0.5, 0.5)
            fovCircleFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
            fovCircleFrame.Size = UDim2.new(0, getgenv().CrackThis.Aiming.FOV * 2, 0, getgenv().CrackThis.Aiming.FOV * 2)
            fovCircleFrame.BackgroundTransparency = 1
            fovCircleFrame.Visible = false
            fovCircleFrame.Parent = screenGui

            local fovCorner = Instance.new("UICorner")
            fovCorner.CornerRadius = UDim.new(1, 0)
            fovCorner.Parent = fovCircleFrame

            local fovStroke = Instance.new("UIStroke")
            fovStroke.Color = Settings.CircleColor
            fovStroke.Thickness = 2.5
            fovStroke.Parent = fovCircleFrame

            local mainFrame = Instance.new("Frame")
            mainFrame.Name = "MainFrame"
            mainFrame.Size = UDim2.new(0, 280, 0, 440)
            mainFrame.Position = UDim2.new(0, 40, 0, 100)
            mainFrame.BackgroundColor3 = Color3.fromRGB(15, 10, 10)
            mainFrame.BackgroundTransparency = 0.05
            mainFrame.Active = true
            mainFrame.Parent = screenGui
            makeDraggable(mainFrame)

            local mainCorner = Instance.new("UICorner")
            mainCorner.CornerRadius = UDim.new(0, 16)
            mainCorner.Parent = mainFrame

            local mainStroke = Instance.new("UIStroke")
            mainStroke.Thickness = 3
            mainStroke.Parent = mainFrame

            task.spawn(function()
                while true do
                    for i = 0, 1, 0.01 do
                        local col = Color3.fromHSV(0, i * 0.5 + 0.5, 1)
                        mainStroke.Color = col
                        task.wait(0.05)
                    end
                    for i = 1, 0, -0.01 do
                        local col = Color3.fromHSV(0, i * 0.5 + 0.5, 1)
                        mainStroke.Color = col
                        task.wait(0.05)
                    end
                end
            end)
-- ================= [END OF PART 3/8] =================

-- ==================================================
-- ABVX LOADER & UNIVERSAL RAYCAST SILENT AIM & CAMLOCK [PART 4/8]
-- ==================================================
            local titleLabel = Instance.new("TextLabel")
            titleLabel.Size = UDim2.new(1, 0, 0, 40)
            titleLabel.BackgroundColor3 = Color3.fromRGB(20, 10, 10)
            titleLabel.TextColor3 = Color3.fromRGB(255, 220, 220)
            titleLabel.TextSize = 13
            titleLabel.Font = Enum.Font.GothamBold
            titleLabel.Text = "🎯 ABVX RAYCAST + CAMLOCK"
            titleLabel.Parent = mainFrame

            local titleCorner = Instance.new("UICorner")
            titleCorner.CornerRadius = UDim.new(0, 16)
            titleCorner.Parent = titleLabel

            local toggleMenuBtn = Instance.new("TextButton")
            toggleMenuBtn.Name = "ToggleMenuButton"
            toggleMenuBtn.Size = UDim2.new(0, 50, 0, 50)
            toggleMenuBtn.Position = UDim2.new(0, 20, 0, 40)
            toggleMenuBtn.BackgroundColor3 = Color3.fromRGB(20, 10, 10)
            toggleMenuBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
            toggleMenuBtn.TextSize = 20
            toggleMenuBtn.Font = Enum.Font.GothamBold
            toggleMenuBtn.Text = "🛡️"
            toggleMenuBtn.Active = true
            toggleMenuBtn.Parent = screenGui
            makeDraggable(toggleMenuBtn)

            local toggleCorner = Instance.new("UICorner")
            toggleCorner.CornerRadius = UDim.new(1, 0)
            toggleCorner.Parent = toggleMenuBtn

            local toggleStroke = Instance.new("UIStroke")
            toggleStroke.Color = Color3.fromRGB(255, 50, 50)
            toggleStroke.Thickness = 2.5
            toggleStroke.Parent = toggleMenuBtn

            toggleMenuBtn.MouseButton1Click:Connect(function()
                isGuiOpen = not isGuiOpen
                mainFrame.Visible = isGuiOpen
            end)

            local scrollingFrame = Instance.new("ScrollingFrame")
            scrollingFrame.Size = UDim2.new(1, -10, 1, -48)
            scrollingFrame.Position = UDim2.new(0, 5, 0, 44)
            scrollingFrame.BackgroundTransparency = 1
            scrollingFrame.CanvasSize = UDim2.new(0, 0, 0, 500)
            scrollingFrame.ScrollBarThickness = 3
            scrollingFrame.Parent = mainFrame

            local uiLayout = Instance.new("UIListLayout")
            uiLayout.Padding = UDim.new(0, 7)
            uiLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
            uiLayout.Parent = scrollingFrame

            local function createButton(initialText, callback)
                local btn = Instance.new("TextButton")
                btn.Size = UDim2.new(1, -10, 0, 38)
                btn.BackgroundColor3 = Color3.fromRGB(25, 12, 12)
                btn.TextColor3 = Color3.fromRGB(255, 220, 220)
                btn.TextSize = 12
                btn.Font = Enum.Font.GothamBold
                btn.Text = initialText
                btn.AutoButtonColor = false
                btn.Parent = scrollingFrame

                local corner = Instance.new("UICorner")
                corner.CornerRadius = UDim.new(0, 10)
                corner.Parent = btn

                local stroke = Instance.new("UIStroke")
                stroke.Color = Color3.fromRGB(200, 50, 50)
                stroke.Thickness = 1.5
                stroke.Parent = btn

                btn.MouseButton1Click:Connect(function()
                    callback(btn)
                end)
                return btn
            end

            createButton("Silent Aim: ON", function(btn)
                getgenv().CrackThis.Aiming.Enabled = not getgenv().CrackThis.Aiming.Enabled
                btn.Text = getgenv().CrackThis.Aiming.Enabled and "Silent Aim: ON" or "Silent Aim: OFF"
            end)

            local camlockGuiBtn = createButton("Camlock Button: OFF", function(btn)
                getgenv().CrackThis.Aiming.CamlockEnabled = not getgenv().CrackThis.Aiming.CamlockEnabled
                btn.Text = getgenv().CrackThis.Aiming.CamlockEnabled and "Camlock Button: ON" or "Camlock Button: OFF"
                
                local existingAvatarBtn = screenGui:FindFirstChild("AvatarCamlockButton")
                if getgenv().CrackThis.Aiming.CamlockEnabled then
                    if not existingAvatarBtn then
                        local avBtn = Instance.new("ImageButton")
                        avBtn.Name = "AvatarCamlockButton"
                        avBtn.Size = UDim2.new(0, 64, 0, 128)
                        avBtn.Position = UDim2.new(1, -90, 0.4, 0)
                        avBtn.BackgroundColor3 = Color3.fromRGB(20, 10, 10)
                        avBtn.Active = true
                        avBtn.Parent = screenGui
                        makeDraggable(avBtn)

                        local avCorner = Instance.new("UICorner")
                        avCorner.CornerRadius = UDim.new(0, 12)
                        avCorner.Parent = avBtn

                        local avStroke = Instance.new("UIStroke")
                        avStroke.Color = Color3.fromRGB(255, 50, 50)
                        avStroke.Thickness = 3
                        avStroke.Parent = avBtn

                        task.spawn(function()
                            pcall(function()
                                local userId = Players:GetUserIdFromNameAsync("aliiiwyddd")
                                local thumbType = Enum.ThumbnailType.HeadShot
                                local thumbSize = Enum.ThumbnailSize.Size420x420
                                local content, isReady = Players:GetUserThumbnailAsync(userId, thumbType, thumbSize)
                                if isReady then
                                    avBtn.Image = content
                                end
                            end)
                        end)

                        avBtn.MouseButton1Click:Connect(function()
                            getgenv().CrackThis.Aiming.CamlockActive = not (getgenv().CrackThis.Aiming.CamlockActive or false)
                            avStroke.Color = getgenv().CrackThis.Aiming.CamlockActive and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 50, 50)
                        end)
                    else
                        existingAvatarBtn.Visible = true
                    end
                else
                    if existingAvatarBtn then
                        existingAvatarBtn.Visible = false
                    end
                    getgenv().CrackThis.Aiming.CamlockActive = false
                end
            end)
-- ================= [END OF PART 4/8] =================

-- ==================================================
-- ABVX LOADER & UNIVERSAL RAYCAST SILENT AIM & CAMLOCK [PART 5/8]
-- ==================================================
            local hitParts = {"HumanoidRootPart", "Head", "UpperTorso", "Torso", "LowerTorso"}
            local hitIndex = 1
            createButton("Hit Part: HumanoidRootPart", function(btn)
                hitIndex = hitIndex % #hitParts + 1
                getgenv().CrackThis.Aiming.HitPart = hitParts[hitIndex]
                btn.Text = "Hit Part: " .. getgenv().CrackThis.Aiming.HitPart
            end)

            local targetModes = {"Closest", "LowHealth"}
            local modeIndex = 1
            createButton("Target: Closest", function(btn)
                modeIndex = modeIndex % #targetModes + 1
                getgenv().CrackThis.Aiming.TargetMode = targetModes[modeIndex]
                btn.Text = "Target: " .. getgenv().CrackThis.Aiming.TargetMode
            end)

            createButton("Prediction: ON (0.03)", function(btn)
                getgenv().CrackThis.Aiming.Prediction = not getgenv().CrackThis.Aiming.Prediction
                btn.Text = getgenv().CrackThis.Aiming.Prediction and "Prediction: ON (0.03)" or "Prediction: OFF"
            end)

            createButton("Infos & Tracers: ON", function(btn)
                getgenv().CrackThis.Aiming.ShowInfo = not getgenv().CrackThis.Aiming.ShowInfo
                getgenv().CrackThis.Aiming.ShowTracers = not getgenv().CrackThis.Aiming.ShowTracers
                btn.Text = getgenv().CrackThis.Aiming.ShowInfo and "Infos & Tracers: ON" or "Infos & Tracers: OFF"
            end)

            createButton("Menu Side: Left", function(btn)
                if Settings.MenuAlign == "Left" then
                    Settings.MenuAlign = "Right"
                    mainFrame.Position = UDim2.new(1, -295, 0, 100)
                    toggleMenuBtn.Position = UDim2.new(1, -75, 0, 40)
                    btn.Text = "Menu Side: Right"
                else
                    Settings.MenuAlign = "Left"
                    mainFrame.Position = UDim2.new(0, 40, 0, 100)
                    toggleMenuBtn.Position = UDim2.new(0, 20, 0, 40)
                    btn.Text = "Menu Side: Left"
                end
            end)

            local sizeBox = Instance.new("TextBox")
            sizeBox.Size = UDim2.new(1, -10, 0, 38)
            sizeBox.BackgroundColor3 = Color3.fromRGB(25, 12, 12)
            sizeBox.TextColor3 = Color3.fromRGB(255, 220, 220)
            sizeBox.TextSize = 12
            sizeBox.Font = Enum.Font.GothamBold
            sizeBox.PlaceholderText = "Type Circle Size (e.g. 160)"
            sizeBox.Text = tostring(getgenv().CrackThis.Aiming.FOV)
            sizeBox.Parent = scrollingFrame

            local boxCorner = Instance.new("UICorner")
            boxCorner.CornerRadius = UDim.new(0, 10)
            boxCorner.Parent = sizeBox

            local boxStroke = Instance.new("UIStroke")
            boxStroke.Color = Color3.fromRGB(200, 50, 50)
            boxStroke.Thickness = 1.5
            boxStroke.Parent = sizeBox

            sizeBox.FocusLost:Connect(function(enterPressed)
                local num = tonumber(sizeBox.Text)
                if num and num > 10 and num < 600 then
                    getgenv().CrackThis.Aiming.FOV = num
                    fovCircleFrame.Size = UDim2.new(0, getgenv().CrackThis.Aiming.FOV * 2, 0, getgenv().CrackThis.Aiming.FOV * 2)
                    sizeBox.Text = tostring(num)
                else
                    sizeBox.Text = tostring(getgenv().CrackThis.Aiming.FOV)
                end
            end)
-- ================= [END OF PART 5/8] =================

-- ==================================================
-- ABVX LOADER & UNIVERSAL RAYCAST SILENT AIM & CAMLOCK [PART 6/8]
-- ==================================================
            local emoteBtn = Instance.new("ImageButton")
            emoteBtn.Name = "EmoteButton"
            emoteBtn.Size = UDim2.new(0, 68, 0, 68)
            emoteBtn.Position = UDim2.new(0, 50, 0, 120)
            emoteBtn.BackgroundColor3 = Color3.fromRGB(15, 12, 12)
            emoteBtn.BackgroundTransparency = 0.15
            emoteBtn.Active = true
            emoteBtn.Parent = screenGui
            makeDraggable(emoteBtn)

            local emoteCorner = Instance.new("UICorner")
            emoteCorner.CornerRadius = UDim.new(1, 0)
            emoteCorner.Parent = emoteBtn

            local emoteStroke = Instance.new("UIStroke")
            emoteStroke.Color = Color3.fromRGB(255, 255, 255)
            emoteStroke.Thickness = 3
            emoteStroke.Parent = emoteBtn

            local emoteGradient = Instance.new("UIGradient")
            emoteGradient.Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 20, 20)),
                ColorSequenceKeypoint.new(0.5, Color3.fromRGB(15, 12, 12)),
                ColorSequenceKeypoint.new(1, Color3.fromRGB(180, 0, 0))
            })
            emoteGradient.Parent = emoteStroke

            pcall(function()
                local content, isReady = Players:GetUserThumbnailAsync(LocalPlayer.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size420x420)
                if content then
                    emoteBtn.Image = content
                end
            end)

            emoteBtn.MouseButton1Click:Connect(function()
                pcall(function()
                    GuiService:SetEmotesMenuOpen(not GuiService:GetEmotesMenuOpen())
                end)
            end)

            RunService.RenderStepped:Connect(function()
                local t = tick() * 2
                local offset = (math.sin(t) + 1) / 2
                emoteGradient.Offset = Vector2.new(offset - 0.5, 0)
            end)
-- ================= [END OF PART 6/8] =================

-- ==================================================
-- ABVX LOADER & UNIVERSAL RAYCAST SILENT AIM & CAMLOCK [PART 7/8]
-- ==================================================
            local topInfoLabel = Instance.new("TextLabel")
            topInfoLabel.Size = UDim2.new(0, 420, 0, 30)
            topInfoLabel.Position = UDim2.new(0.5, -210, 0, 10)
            topInfoLabel.BackgroundTransparency = 1
            topInfoLabel.TextColor3 = Color3.fromRGB(255, 220, 220)
            topInfoLabel.TextSize = 14
            topInfoLabel.Font = Enum.Font.GothamBold
            topInfoLabel.TextStrokeTransparency = 0
            topInfoLabel.Text = "TARGET INFO: NONE"
            topInfoLabel.Visible = false
            topInfoLabel.Parent = screenGui

            local tracerFrame = Instance.new("Frame")
            tracerFrame.Name = "TracerLine"
            tracerFrame.AnchorPoint = Vector2.new(0.5, 0.5)
            tracerFrame.BackgroundColor3 = Color3.fromRGB(255, 80, 80)
            tracerFrame.BorderSizePixel = 0
            tracerFrame.Visible = false
            tracerFrame.Parent = screenGui

            local function getTarget()
                local bestTarget = nil
                local bestMetric = math.huge
                local screenCenter = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y / 2)

                local function evaluate(char)
                    if char and char ~= LocalPlayer.Character then
                        local humanoid = char:FindFirstChildOfClass("Humanoid")
                        local targetPart = char:FindFirstChild(getgenv().CrackThis.Aiming.HitPart) or char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Head")
                        if humanoid and humanoid.Health > 0 and targetPart then
                            local screenPoint, onScreen = camera:WorldToViewportPoint(targetPart.Position)
                            if onScreen then
                                local screenPos2D = Vector2.new(screenPoint.X, screenPoint.Y)
                                local distFromCenter = (screenPos2D - screenCenter).Magnitude
                                
                                if distFromCenter <= getgenv().CrackThis.Aiming.FOV then
                                    local mode = getgenv().CrackThis.Aiming.TargetMode or "Closest"
                                    if mode == "Closest" then
                                        if distFromCenter < bestMetric then
                                            bestMetric = distFromCenter
                                            bestTarget = char
                                        end
                                    elseif mode == "LowHealth" then
                                        if humanoid.Health < bestMetric then
                                            bestMetric = humanoid.Health
                                            bestTarget = char
                                        end
                                    end
                                end
                            end
                        end
                    end
                end

                pcall(function()
                    for _, p in ipairs(Players:GetPlayers()) do
                        if p.Character then evaluate(p.Character) end
                    end

                    for _, folderName in ipairs({"Bots", "Bots_1", "NPCs", "Dummies", "Targets", "Dummy", "Enemies", "Players", "CharacterFolder"}) do
                        local folder = workspace:FindFirstChild(folderName)
                        if folder then
                            for _, obj in ipairs(folder:GetChildren()) do
                                if obj:IsA("Model") then evaluate(obj) end
                            end
                        end
                    end
                end)

                return bestTarget
            end

            local currentTargetModel = nil
            local lockedHitPart = nil
            local smoothedPosition = Vector3.new(0, 0, 0)

            RunService.RenderStepped:Connect(function(dt)
                pcall(function()
                    if not getgenv().CrackThis.Aiming.Enabled then
                        currentTargetModel = nil
                        lockedHitPart = nil
                        fovCircleFrame.Visible = false
                        topInfoLabel.Visible = false
                        tracerFrame.Visible = false
                        return
                    end

                    fovCircleFrame.Visible = getgenv().CrackThis.Aiming.FOVCircle
                    
                    currentTargetModel = getTarget()

                    if currentTargetModel then
                        local partName = getgenv().CrackThis.Aiming.HitPart or "HumanoidRootPart"
                        lockedHitPart = currentTargetModel:FindFirstChild(partName) or currentTargetModel:FindFirstChild("HumanoidRootPart") or currentTargetModel:FindFirstChild("Head")
                    else
                        lockedHitPart = nil
                    end

                    if getgenv().CrackThis.Aiming.CamlockEnabled and getgenv().CrackThis.Aiming.CamlockActive and currentTargetModel and lockedHitPart then
                        local camTargetPos = lockedHitPart.Position
                        local currentCamPos = camera.CFrame.Position
                        local targetCFrame = CFrame.new(currentCamPos, camTargetPos)
                        local smoothnessVal = getgenv().CrackThis.Aiming.Smoothness or 10
                        local alpha = math.clamp(dt * smoothnessVal, 0, 1)
                        camera.CFrame = camera.CFrame:Lerp(targetCFrame, alpha)
                    end

                    if currentTargetModel and lockedHitPart then
                        fovStroke.Color = Color3.fromRGB(0, 255, 0)

                        local rawTargetPos = lockedHitPart.Position
                        local linearVelocity = lockedHitPart.AssemblyLinearVelocity or Vector3.new(0, 0, 0)
                        local pred = getgenv().CrackThis.Aiming.Prediction and 0.03 or 0
                        local predictedPos = rawTargetPos + Vector3.new(linearVelocity.X * pred, linearVelocity.Y * pred, linearVelocity.Z * pred)

                        if smoothedPosition == Vector3.new(0, 0, 0) then
                            smoothedPosition = predictedPos
                        else
                            local smoothnessVal = getgenv().CrackThis.Aiming.Smoothness or 10
                            local smoothFactor = math.clamp(dt * smoothnessVal, 0, 1)
                            smoothedPosition = smoothedPosition:Lerp(predictedPos, smoothFactor)
                        end

                        local humanoid = currentTargetModel:FindFirstChildOfClass("Humanoid")
                        local rootPart = currentTargetModel:FindFirstChild("HumanoidRootPart") or lockedHitPart
                        
                        if humanoid and humanoid.Health > 0 then
                            local distanceMeters = 0
                            if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                                distanceMeters = math.floor((LocalPlayer.Character.HumanoidRootPart.Position - rootPart.Position).Magnitude / 3.5)
                            end

                            local targetPlayer = Players:GetPlayerFromCharacter(currentTargetModel)
                            local nameStr = targetPlayer and targetPlayer.DisplayName or currentTargetModel.Name

                            if getgenv().CrackThis.Aiming.ShowInfo then
                                topInfoLabel.Text = string.format("TARGET: %s | HP: %d | DIST: %dm", nameStr, math.floor(humanoid.Health), distanceMeters)
                                topInfoLabel.Visible = true
                            else
                                topInfoLabel.Visible = false
                            end

                            if getgenv().CrackThis.Aiming.ShowTracers then
                                local screenPos, onScreen = camera:WorldToViewportPoint(smoothedPosition)
                                if onScreen then
                                    local screenCenterBottom = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y)
                                    local targetPos2D = Vector2.new(screenPos.X, screenPos.Y)
                                    local distance = (targetPos2D - screenCenterBottom).Magnitude
                                    local midpoint = (screenCenterBottom + targetPos2D) / 2
                                    
                                    tracerFrame.Size = UDim2.new(0, distance, 0, 2)
                                    tracerFrame.Position = UDim2.new(0, midpoint.X, 0, midpoint.Y)
                                    tracerFrame.Rotation = math.deg(math.atan2(targetPos2D.Y - screenCenterBottom.Y, targetPos2D.X - screenCenterBottom.X))
                                    tracerFrame.Visible = true
                                else
                                    tracerFrame.Visible = false
                                end
                            else
                                tracerFrame.Visible = false
                            end
                        else
                            lockedHitPart = nil
                            currentTargetModel = nil
                            topInfoLabel.Visible = false
                            tracerFrame.Visible = false
                        end
                    else
                        fovStroke.Color = Settings.CircleColor
                        topInfoLabel.Visible = false
                        tracerFrame.Visible = false
                        smoothedPosition = Vector3.new(0, 0, 0)
                    end
                end)
            end)
-- ================= [END OF PART 7/8] =================

-- ==================================================
-- ABVX LOADER & UNIVERSAL RAYCAST SILENT AIM & CAMLOCK [PART 8/8]
-- ==================================================
            local function getTargetPosition()
                if not lockedHitPart then
                    return Vector3.new()
                end
                if smoothedPosition == Vector3.new(0, 0, 0) then
                    return lockedHitPart.Position
                end
                return smoothedPosition
            end

            local function findFramework()
                local fw = LocalPlayer.PlayerGui:FindFirstChild("_Framework") 
                    or LocalPlayer.PlayerGui:FindFirstChild("Framework")
                    or workspace:FindFirstChild("_Framework")
                    or workspace:FindFirstChild("Framework")
                    or game:GetService("ReplicatedStorage"):FindFirstChild("_Framework")
                    or game:GetService("ReplicatedStorage"):FindFirstChild("Framework")
                return fw
            end

            local _Framework = findFramework()
            local frameworkEnv = _Framework and pcall(function() return getsenv(_Framework) end) and getsenv(_Framework) or nil

            RunService.PostSimulation:Connect(function()
                pcall(function()
                    if getgenv().CrackThis.Aiming.Enabled and lockedHitPart then
                        if frameworkEnv and frameworkEnv._G then
                            frameworkEnv._G.MOUSE_POSITION = getTargetPosition()
                        end
                    end
                end)
            end)

            pcall(function()
                local mt = getrawmetatable(game)
                setreadonly(mt, false)
                local oldNamecall = mt.__namecall

                mt.__namecall = newcclosure(function(self, ...)
                    local method = getnamecallmethod()
                    local args = {...}

                    if getgenv().CrackThis.Aiming.Enabled and lockedHitPart then
                        if method == "FireServer" or method == "InvokeServer" then
                            local targetPos = getTargetPosition()
                            for i, arg in ipairs(args) do
                                local t = typeof(arg)
                                if t == "Vector3" then
                                    args[i] = targetPos
                                elseif t == "CFrame" then
                                    args[i] = CFrame.new(arg.Position, targetPos)
                                elseif t == "table" then
                                    for k, v in pairs(arg) do
                                        if typeof(v) == "Vector3" then
                                            arg[k] = targetPos
                                        elseif typeof(v) == "CFrame" then
                                            arg[k] = CFrame.new(v.Position, targetPos)
                                        end
                                    end
                                end
                            end
                            return oldNamecall(self, unpack(args))
                        end
                    end

                    return oldNamecall(self, ...)
                end)
            end)

            pcall(function()
                local oldRaycast
                oldRaycast = hookfunction(workspace.Raycast, function(ws, origin, direction, raycastParams)
                    if getgenv().CrackThis.Aiming.Enabled and lockedHitPart then
                        local originPos = (typeof(origin) == "Vector3") and origin or camera.CFrame.Position
                        local targetPos = getTargetPosition()
                        local newDirection = (targetPos - originPos)
                        return oldRaycast(ws, originPos, newDirection, raycastParams)
                    end
                    return oldRaycast(ws, origin, direction, raycastParams)
                end)
            end)

            pcall(function()
                local oldFindRay
                oldFindRay = hookfunction(workspace.FindPartOnRay, function(ws, ray, ign, tn, ct)
                    if getgenv().CrackThis.Aiming.Enabled and lockedHitPart then
                        local targetPos = getTargetPosition()
                        local newRay = Ray.new(ray.Origin, (targetPos - ray.Origin).Unit * ray.Direction.Magnitude)
                        return oldFindRay(ws, newRay, ign, tn, ct)
                    end
                    return oldFindRay(ws, ray, ign, tn, ct)
                end)
            end)
        end
    end
end)

print("✅ ABVX UNIVERSAL RAYCAST SILENT AIM & CAMLOCK LOADED! | TikTok: .vfsv")
-- ================= [END OF PART 8/8] =================
